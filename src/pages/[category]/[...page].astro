---
import type { ComponentProps } from 'astro/types'
import CategoryListing from '../../components/pages/CategoryListing.astro'
import { categoryListPaths } from '../../lib/routes'

type Props = ComponentProps<typeof CategoryListing>

export const getStaticPaths = categoryListPaths('pt')
---

<CategoryListing {...Astro.props} />
