---
import type { ComponentProps } from 'astro/types'
import TagListing from '../../../../components/pages/TagListing.astro'
import { tagListPaths } from '../../../../lib/routes'

type Props = ComponentProps<typeof TagListing>

export const getStaticPaths = tagListPaths('en')
---

<TagListing {...Astro.props} />
