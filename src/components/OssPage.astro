---
import { alternatesFor, type Locale, t } from '../i18n/ui'
import BaseLayout from '../layouts/BaseLayout.astro'
import CreditList from './CreditList.astro'

interface Props {
  locale: Locale
}

const { locale } = Astro.props
---

<BaseLayout
  title={`LS: ${t(locale, 'ossTitle')}`}
  description={t(locale, 'ossDescription')}
  lang={locale}
  alternates={alternatesFor('/oss/', Astro.site)}
>
  <h1 class="listing-heading">{t(locale, 'ossTitle')}</h1>
  <p class="intro listing-heading">{t(locale, 'ossIntro')}</p>
  <div class="listing-heading">
    <CreditList locale={locale} />
  </div>
</BaseLayout>

<style>
  .intro {
    margin-block-end: 2.5rem;
    color: var(--muted);
  }
</style>
