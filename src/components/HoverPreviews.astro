---
// Drop this once anywhere in the page (BaseLayout is the natural spot). It
// takes no props: at runtime it scans <article a href> and attaches hover /
// long-press preview cards. With JS disabled every link stays a plain,
// fully working <a href> — this only ever adds behaviour on top.
import '../styles/hover-previews.css'
---

<script>
  import '../scripts/hover-previews'
</script>
